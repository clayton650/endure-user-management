version: 0.2

phases:
  build:
    commands:
      - |
        IFS=',' read -ra FUNCTION_NAMES <<< "$FUNCTION_NAMES"
        for i in "${FUNCTION_NAMES[@]}"; do
          aws lambda update-function-code --function-name $i --zip-file fileb://$KEY
        done
